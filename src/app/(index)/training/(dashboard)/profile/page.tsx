import { type NextPage } from 'next';

// The layout is basically the main page for the /profile endpoint.
// Only the [cid] nested page inserts additional content.
const StudentProfile: NextPage = () => undefined;

export default StudentProfile;
