import { type NextPage } from 'next';

// The layout is basically the main page for the /mentor endpoint.
// Only the [cid] nested page inserts additional content.
const MentorProfile: NextPage = () => undefined;

export default MentorProfile;
