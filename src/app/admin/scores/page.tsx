import { type NextPage } from 'next';

// The layout is basically the main page for the /scores endpoint.
// Only the [cid] nested page inserts additional content.
const EventScores: NextPage = () => undefined;

export default EventScores;
